package callisto_config

APP_NAME        : string        : "Callisto Application"
// In semantic versioning form Major, Minor, Patch. E.g. v1.0.3 -> {1, 0, 3}
APP_VERSION     : [3]u32        : {0, 0, 1}
