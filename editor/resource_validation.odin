package callisto_editor

// Create an AST of the app
// Look for Resource[$T]("res://uri") nodes
// Check that the URI exists
// Check that the URI contains a resource with the correct type

// Check any additional validation tags on the Resource node?
